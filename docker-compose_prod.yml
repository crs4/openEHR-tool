version: '2'
services:
    web:
        build: 
          context: .
          dockerfile: ./Dockerfile.prod
        image: surfercrs4/openehrtoolprod
        ports:
            - "9000:9000"
        network_mode: host
        env_file:
            - ./.env.prod